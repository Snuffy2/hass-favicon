{
  "title": "Favicon",
  "config": {
    "error": {
      "bad_path": "Icon path must start with /local/"
    },
    "step": {
      "user": {
        "title": "Favicon changer",
        "description": "Set the page title, path to icons, and launch icon color.",
        "data": {
          "title": "Page title",
          "icon_path": "Icon path (must start with /local/)",
          "launch_icon_color": "Launch icon color"
        }
      }
    },
    "abort": {
      "single_instance_allowed": "Only a single configuration of favicon is allowed."
    }
  },
  "options": {
    "error": {
      "bad_path": "Icon path must start with /local/"
    },
    "step": {
      "init": {
        "data": {
          "title": "Page title",
          "icon_path": "Icon path (must start with /local/)",
          "launch_icon_color": "Launch icon color"
        }
      }
    }
  }
}
